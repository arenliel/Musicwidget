<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema para el selector de widgets (Soporte Material 3) -->
    <!-- Tema refinado siguiendo el estándar de Metrolist (API 31+) -->
    <style name="Theme.MusicWidget" parent="@android:style/Theme.DeviceDefault.NoActionBar" />

    <!-- Tema para actividades invisibles/trampolines (Elimina el flicker negro) -->
    <style name="Theme.MusicWidget.Trampoline" parent="Theme.MusicWidget">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="Theme.MusicWidget.Viewer" parent="Theme.MusicWidget">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>